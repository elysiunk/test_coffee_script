// Generated by CoffeeScript 1.6.3
(function() {
  ContactManager.module("ContactsApp.List", function(List, ContactManager, Backbone, Marionette, $, _) {
    return List.Controller = {
      listContacts: function() {
        var contacts, contactsListView;
        contacts = ContactManager.request("contact:entities");
        contactsListView = new List.Contacts({
          collection: contacts
        });
        ContactManager.mainRegion.show(contactsListView);
        contactsListView.on("itemview:contact:show", function(childView, model) {
          return ContactManager.trigger("contact:show", model.get("id"));
        });
        contactsListView.on("itemview:contact:modal", function(childView, model) {
          return ContactManager.ContactsApp.Modal.Controller.modalContact(model);
        });
        return contactsListView.on("itemview:contact:edit", function(childView, model) {
          return ContactManager.ContactsApp.Edit.Controller.editContact(model);
        });
      }
    };
  });

}).call(this);
